#include<iostream>
using namespace std;
int main(){
    float r,Area;
    const float p=3.14;
    cout<<"Calculate the area of a circle for a given value of radius (r), where area of circle Area=p*(r*r)\n\n";
    cout<<"Enter the radius: ";
    cin>>r;
    Area=p*(r*r);
    cout<<"The area of a circle with a radius of "<<r<<" is equal to : "<<Area<<endl;






}